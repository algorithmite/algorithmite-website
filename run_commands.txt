/* compile and pack client wasm */

cd client

/* replace --dev with --release for wasm minification */
wasm-pack build --target web --dev
rollup -c
uglifyjs -c -o ./pkg/bundle.min.js -- ./pkg/bundle.js

cd ..

cd server

cargo run
